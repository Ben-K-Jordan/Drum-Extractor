# Quick start: the package (core deps) plus Stage 1 separation — the MVP.
# Single source of truth for versions is pyproject.toml; this file is only a
# convenience pointer so `pip install -r requirements.txt` does the right thing.
#
# Full pipeline:  pip install -e ".[all]"
# Drum backend:   pip install -e ".[adtof]"   (see README for MuseScore/madmom)
-e .[separation]
