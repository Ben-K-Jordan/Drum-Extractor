# Minimal quick-start: the always-works core + Stage 1 separation (the MVP).
# For the full pipeline, prefer the extras:  pip install -e ".[all]"
# and see the README for ADTOF, madmom, and MuseScore setup.

numpy>=1.22
pretty_midi>=0.2.10
demucs>=4.0.0
